Before executing the code you must have Pandas, Matplotlib and NumPy libraries installed.

Default Parameters:
alpha = 1
log(0) = -10^12

You can exectue the program with this command below:
python q3main.py

The outputs are classified by question sections. You can see which output belongs to which question on top of the output.